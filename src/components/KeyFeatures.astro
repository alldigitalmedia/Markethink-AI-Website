<section class="section light-green key-features-section" id="system-foundation">
  <div class="container">
    <div class="section-header text-center animate-fade-up">
      <p class="subtitle">THE MARKETHINK DIFFERENCE</p>
      <h2>A marketing system that learns your brand<br />and keeps improving with the technology.</h2>
      <p class="section-description">
        Markethink is built for businesses that want better marketing output without becoming AI experts. It keeps your brand context, campaigns, and approvals in one place while the platform improves as better models and creative tools become available.
      </p>
    </div>
    <div class="feature-system-visual animate-fade-up">
      <img src="/images/key-feature-markethink-input-output-flow.avif" alt="Markethink system turning inputs into marketing outputs" />
    </div>
    <div class="feature-copy-grid animate-stagger">
      <div class="cta-card-overlay light">
        <h3>Turn better context into better campaigns</h3>
        <p>
          Markethink learns your audience, offers, voice, and approval patterns so each campaign starts from what your business already knows instead of a blank prompt.
        </p>
        <a href="/#how-it-works" class="button">See the process</a>
      </div>
      <div class="cta-card-overlay dark">
        <h3>Benefit from new AI without chasing it</h3>
        <p>
          As new models, image tools, and workflows improve, Markethink keeps evolving in the background so your marketing can keep getting sharper too.
        </p>
        <a href="/#workspace" class="button is-secondary">See the workspace</a>
      </div>
    </div>
  </div>
</section>

<style>
  .section-description {
    font-size: 1.125rem;
    font-weight: 500;
    color: rgba(15, 18, 19, 0.8);
    margin-top: 1rem;
    max-width: 46rem;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  .key-features-section .subtitle {
    color: rgba(0, 0, 0, 0.5);
  }

  .key-features-section h2 {
    color: var(--text-primary);
  }

  .feature-system-visual {
    margin-top: 3rem;
    display: flex;
    justify-content: center;
  }

  .feature-system-visual img {
    width: min(1040px, 100%);
    height: auto;
    border-radius: var(--radius-medium);
    transition: transform 0.6s ease;
  }

  .feature-system-visual:hover img {
    transform: scale(1.03);
  }

  .feature-copy-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .cta-card-overlay {
    border-radius: var(--radius-medium);
    padding: 2rem 2.5rem;
    margin-top: 1rem;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .cta-card-overlay.light {
    background: var(--tabs-bg);
    border: 1px solid var(--neutral-200);
  }

  .cta-card-overlay.light h3 {
    color: var(--text-primary);
  }

  .cta-card-overlay.light p {
    color: rgba(15, 18, 19, 0.56);
  }

  .cta-card-overlay.dark {
    background: var(--bg-primary);
    border: 1px solid rgba(217, 217, 218, 0.2);
  }

  .cta-card-overlay.dark h3 {
    color: var(--text-invert);
  }

  .cta-card-overlay.dark p {
    color: rgba(255, 255, 255, 0.6);
  }

  .cta-card-overlay h3 {
    font-family: var(--font-body);
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .cta-card-overlay p {
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  @media (max-width: 767px) {
    .feature-copy-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
